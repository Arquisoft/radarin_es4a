"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startDevServer = void 0;
const debug_1 = require("debug");
const startServer_1 = require("./startServer");
const debug = debug_1.debug('cypress:webpack-dev-server:webpack');
function startDevServer(startDevServerArgs, exitProcess = process.exit) {
    return __awaiter(this, void 0, void 0, function* () {
        const webpackDevServer = yield startServer_1.start(startDevServerArgs, exitProcess);
        return new Promise((resolve) => {
            const httpSvr = webpackDevServer.listen(0, '127.0.0.1', () => {
                // FIXME: handle address returning a string
                const port = httpSvr.address().port;
                debug('Component testing webpack server started on port', port);
                resolve({
                    port,
                    close: (done) => {
                        httpSvr.close();
                        done === null || done === void 0 ? void 0 : done();
                    },
                });
            });
        });
    });
}
exports.startDevServer = startDevServer;
